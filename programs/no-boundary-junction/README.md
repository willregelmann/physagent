# Self-Consistency at a No-Boundary Signature-Change Junction

**Status:** Early notes (experimenter-priority program, promoted directly on 2026-09-05; tracking issue #215)

## In plain English

Take the framework's timeless, purely spatial four-dimensional block and read it as the "Euclidean cap" of a no-boundary universe: a smooth, edgeless region with no time direction, out of which ordinary spacetime histories emerge across a border where the time direction switches on. This program asks what the framework's own consistency condition says *at that border* — which surface it is, what it fixes, and what it leaves free. So far the record establishes mostly what the border does not do. It does not by itself produce any energy scale other than the one the framework already had (the curvature of the cap), so it cannot explain on its own why particle masses are so far below that scale; what it would need is a dimensionless coupling fixed at the border, and the record contains no rule that fixes one (established in a structured three-way debate, each line surviving adversarial review only as this negative). The cap the framework's own equations produce is at the Planck scale for realistic matter content and becomes trustworthy only with enormously many fields, an input this program states rather than derives (a rigorous arithmetic fact). How long the emerging universe lingers near the cap before its instability takes over is not a property of the cap either; it is set by a second free number in the framework's gravitational sector (a sketch-level result). The program's first real question was therefore whether the border constrains that second number. Comparing the two known symmetric caps suggests a ceiling under one rule for adding them up, and a perturbation calculation adds a floor; both the resulting window and the short inflation estimate are conditional on which caps contribute and which histories count (sketch-level). The two candidate rules have now been built and stress-tested, but neither has an established most likely history. The two calculations fix different kinds of boundary data, and the follow-up work has now defined both problems properly and found the correction term that relates them. With it, the supposed lower-action competitor to the standard cap loses at every late time; it keeps a small edge only right at the border and only in part of the allowed range (a rigorous numerical result). Two arguments that had seemed to favour one rule over the other turn out to favour neither: whether the sum over real shapes stays finite depends on which boundary data one fixes, not on the rule, and each rule has its own family of runaway shapes near the pole. One boundary choice does single out the border surface itself as the most probable real shape (sketch-level), a lead for a later milestone. Which caps contribute therefore depends on a single missing object, the integration contour for this theory's time variable, which no calculation on record has yet fixed; constructing it is the next milestone. Whether nearly classical histories enlarge the set remains open. The border also supplies a direction of time on each emerging history, but only the one already contained in its causal structure; the branching-tree picture restates it rather than deriving it (sketch-level). Everything about deriving quantum mechanics, time, or the arrow of time from the border remains conjecture and is labelled as such.

## Scope

Self-consistency of the semiclassical Einstein equation at a signature-change junction between a no-boundary Euclidean cap (the framework's anomaly-driven de Sitter fixed point, continued to the round S⁴) and the Lorentzian classical branches that emerge from it. The program is:

- **downstream of `signature-change-boundary`**, whose fixed-background results (benign crossing; junction regularity; in the smooth class, vanishing extrinsic curvature) it uses as kinematic input and does not re-derive;
- **downstream of `fixed-point-existence`**, whose exact anomaly-driven fixed point supplies the cap (existence and instability Rigorous by citation; coefficient Sketch, with the convention now reconciled — see `notes/2026-09-05-step0-which-junction.md` §1);
- **upstream of `co-emergence`'s** mass, local-time and local-Hilbert-space questions, to which it hands junction-side results (the reframing's proposals for Osterwalder–Schrader reconstruction across the derived junction, WKB time on the branches, and a causal-order orientation are conjectures here, milestoned as J-4 to J-6).

**Standing assumption of the program (explicit input, not a result):** conformal field content with a₂ ≡ (N_S + 11N_F + 62N_V)/2 ≥ 10⁶, so that the cap is semiclassical (H₀ ≤ 0.1 M̄_P). Results depending on a₂ are stated as functions of a₂ with validity ranges. The alternative junctions — the observed cosmological constant (not a fixed point of anything on record) and a Planckian cap with Standard-Model content (outside semiclassical validity) — were considered and rejected at step 0.

## Current contents

- `notes/2026-09-08-j7a-boundary-problems-defined.md` — J-7a: the fourth-order boundary problems defined, attacked and defended. The frame-invariant cut action is the fixed-(a,v) action with v-momentum (ε/4)a²R (Hamilton–Jacobi at 8 points; independent off-shell derivation); the fixed-(q,R) action is I_inv − (ε/4)a²Rv (Dyer–Hinterbichler's F′(R)K term) and the two wavefunctions are a Fourier pair over the real Lorentzian expansion rate. With the term the J-6 recontracting-cap advantage survives only at the junction data for 1/4 < ε < 0.330 (≤ 0.61a) and is refuted at every large three-sphere (Rigorous (numerical)). The real-family boundedness asymmetry of J-6 §1.2 reverses between representations, so it is not a sign discriminator; the (h,R) problem's natural boundary condition selects the K = 0 cut (Sketch; J-3 input). Neither sign is protected against near-conical off-shell data (the tunneling sign fails on needle cones for ε < 1). One contour is missing — the fourth-order lapse contour of the (a,v) problem — and constructing it is J-7b.
- `notes/2026-09-06-j6-saddle-commitment-two-paths.md` — J-6 debate synthesis, corrected September 8, 2026 (§9): saddle prescription deferred. The H window 1/4 < ε < ε_c is a conditional comparison of the two known symmetric instantons. The H recontracting solution is at fixed (q,R); the T complex census is at fixed (a,v), v = da/dτ. J-7 must specify separate off-shell integrals and boundary actions. FLT bounds individual exponential factors, not the full wavefunction; exactly-real solutions found numerically do not establish a complete classical ensemble. Orientation is re-expression of causal order and the record arrow (Sketch).
- `notes/2026-09-06-j1-root-uniqueness-bounds-the-boxR-coefficient.md` — J-1: the junction constrains the □R / R² coefficient through root dominance: the sphere is a real junction for every ε, but above ε_c ∈ (0.35, 0.36) a symmetric double bubble is a second real junction with lower Euclidean action whose continuation recollapses; the two-known-instanton comparison favors the sphere below ε_c and yields a seed-based short depth estimate (Rigorous (numerical) for the caps and actions; Sketch for dominance, conditional on weighting, relevance and the restricted ensemble; J-6 §9 leaves completeness open).
- `notes/2026-09-05-step0-which-junction.md` — step 0 of #215: the two coefficient conventions reconciled (the recorded 16π discrepancy is 8π × 2; Rigorous arithmetic); the junction is Planckian for Standard-Model content and semiclassical only for a₂ ≳ 1.4 × 10⁶ (Rigorous arithmetic; threshold a convention); the de Sitter depth of the root is N_inf ≈ 3(H₀²/M²) ln(M_P/H₀), set by the free □R / R² coefficient that also defines the scalaron mass M — not by the fixed point (algebra Rigorous, seed estimate Sketch); commitment to the anomaly junction with large field content.
- The explorer synthesis that produced the program, `programs/co-emergence/explorations/2026-09-05-junction-mass-scale.md` (PR #216): the mass test answered "no from (Λ, G) alone" with the residue named (one dimensionless coupling per additional scale); the elimination ledger; and three side results (toy-model fixed-point uniqueness, Rigorous; period cap at 2, Rigorous; smooth-class junction condition, Rigorous given background). Full record on `scratch/explorer/2026-09-05-junction-mass-scale`.

## Key results

| Result | Status | Source |
|---|---|---|
| The no-boundary junction with free matter carries exactly one continuous dimensionless number, ε_J = (H₀/M_P)² = 180π/a₂; every other branch scale is M_P × (function of a coupling); no selection principle for any coupling is on record | Sketch (components Rigorous) | #216 synthesis §3 |
| K_Σ = 0 is forced in both gluings (reality condition; Hayward/Kossowski–Kriele and curvature regularity in the smooth class) | Rigorous, given fixed background and smoothness | #216 synthesis §3, §6c |
| H₀² = 180π/(G a₂) = 2880π² M̄_P²/(N_S + 11N_F + 62N_V); the printed coefficient is standard | Rigorous arithmetic, given the standard anomaly coefficients | step-0 note §1 |
| Standard-Model content gives H₀ ≈ 5.3 M̄_P; semiclassical cap needs a₂ ≳ 1.4 × 10⁶ | Rigorous arithmetic; threshold Sketch | step-0 note §1.3 |
| Linearized instability of the cap's de Sitter branch: (λ + 4)(ελ² + 3ελ − 1) = 0, ε = H₀²/M², one growing mode for one sign of the □R coefficient | Rigorous (flat-slice trace-equation linearization, sympy) | step-0 note §2.2 |
| N_inf ≈ 3(H₀²/M²) ln(M_P/H₀): the root's depth is a function of the free □R coefficient | Sketch | step-0 note §2.3 |
| For the unstable sign ε > 0 and ε > ε_c ∈ (0.35, 0.36), a second reflection-symmetric regular Euclidean cap exists (the symmetric double bubble); for ε ≤ 0.35 and for ε < 0 the sphere is the only real junction | Rigorous (numerical), minisuperspace | J-1 note §2 |
| The double bubble's Euclidean action lies below the sphere's by a small finite gap at birth (ΔI ≈ −0.12a, corrected 2026-09-06 from an earlier −2.7a) growing to ≈ −3.3εa; its real continuation recollapses within a Hubble time; the minisuperspace action normalization is exact (Euler–Lagrange ≡ trace equation); every compact regular cap has I_EH = −2a by a scaling identity | Rigorous (numerical); normalization and scaling identity Rigorous | J-1 note §3 |
| J-1's two-known-instanton comparison gives an upper bound ε < ε_c ≈ 0.355 under Euclidean weighting; the seed-based depth estimate is N_inf ≈ 5–7 | Sketch, conditional on relevance and restricted ensemble; J-6 adds the lower threshold and leaves completeness open | J-1 note §4; J-6 §2, §9 |
| The flat 4-ball is a regular saddle with local action −(3/4)a q₁; for ε > 0 the local action of real regular caps reaching a fixed radius is unbounded below, so the Hartle–Hawking weight has no minimum on the real family while the tunneling weight's supremum there is at the flat ball | Rigorous (numerical), three independent codes | J-6 synthesis §1.2 |
| Fixed-(a,v) search: sphere is the only exactly-real Lorentzian continuation identified among ≥ 18 complex competitors; in the frame-invariant action, on the five saddles that pass the constant-lapse test, the sphere is extremal under neither sign (A 2.6a below it under e^{−I}, D 8.5a above it under e^{+I}; seven values sheet-conditional, six gauges unrepaired). Fixed-(q,R) search: with the fixed-R boundary term the real recontracting cap is lower than the sphere only for 1/4 < ε < 0.330 at q = 4 (by ≤ 0.61a) and only for q < q*(ε) ≤ 9.5; beyond that the sphere is lower with a monotonically growing gap (12.4a at (ε, q) = (1, 4); +144a at q = 15); the complex pair stays 0.1–1.4a below the sphere and is excluded only by the Sketch-level sheet criterion | Rigorous (numerical) for the tables; relevance, completeness and the pair's exclusion open | J-6 synthesis §1.3–1.4, §9; J-7a note §4, §7 |
| The frame-invariant cut action is the fixed-(a,v) variational action, with v-momentum Q = (ε/4)a²R (the R² Gibbons–Hawking momentum; the anomaly sector contributes none) and a-momentum equal to the ESU-frame Ostrogradsky momentum; the fixed-(q,R) action is I_inv − (ε/4)a²Rv (Dyer–Hinterbichler's F′(R)K term); the two are a Legendre pair and the wavefunctions a Fourier pair over the real Lorentzian expansion rate v ∈ iℝ | Rigorous (numerical, 8 Hamilton–Jacobi points; independent off-shell derivation); wavefunction relation Sketch | J-7a note §2–3 |
| On the real regular family at fixed a the on-shell fixed-(q,R) action is bounded below with its minimum exactly at the K = 0 cut (−4.45 at a = 2, ε = 1) while the fixed-(a,v) action is unbounded below with its maximum at the flat ball: J-6 §1.2's boundedness asymmetry is representation-dependent and not a sign discriminator; the K = 0 cut is the natural-boundary-condition saddle of the (h,R) problem; the K = 0 cap's weight then grows without bound in the radius (runaway relocated, not removed) | Rigorous (numerical) for the family; K = 0 selection Sketch (J-3 input) | J-7a note §6 |
| On the Euclidean-real-section contour (real Euclidean cut data) the (q,R) transform diverges under e^{−I} and converges on the real-cap branch under e^{+I}, the recontracting cap being relevant under both signs there; the integrand is multi-branch (a constant-lapse complex pair 72a above the real cap at the same data), and the canonical contour is imaginary v, on which the (q,R) problem reduces to the (a,v) problem plus a Fourier transform | Rigorous (numerical) on the real-cap branch; branch sum Conjecture; framing Sketch | J-7a note §5 |
| Near-conical off-shell data (regular-pole smoothings of a ≈ vτ) have action c ln(1/δ) with c(v) − c(1) = −(v−1)²[3ε(v+1)² + (v−1)(v+3)]/(4v): unbounded below in every neighbourhood of the regular pole (Hartle–Hawking sign) and unbounded above on needle cones v < 2/√(1+3ε) − 1 (tunneling sign, ε < 1); no pole condition bounds either sign, so both states must exclude the near-conical locus by their contour | Rigorous (sympy + numerical), minisuperspace, scheme-independent | J-7a note §8 |
| FLT excludes enhanced semiclassical exponential factors for relevant saddles when Re(iS) vanishes on the defining cycle; this does not establish a bounded wavefunction | Rigorous conditional statement by citation; full-state boundedness unproved | J-6 synthesis §3.1, §3.4, §9 |
| Under e^{−I} the sphere has Gaussian tensors and only the homogeneous negative mode iff ε > 1/4 (HHR's spectrum with α_HHR = −ε/2, derived three ways): the restricted two-instanton comparison gives 1/4 < ε < ε_c under the Hartle–Hawking sign and no upper bound under the tunneling sign | Rigorous by citation with derived map; window Sketch, conditional on relevance and the restricted ensemble; sign unresolved | J-6 synthesis §1.5, §2 |
| The double bubble has a second real history: an open Hawking–Turok-type continuation through its pole, super-inflating for N_open ≈ 1.6–95 e-folds (ε = 0.36–20) at the same weight, ending in a finite-time singularity | Rigorous (numerical) | J-6 synthesis §1.10 |
| The anomaly action on a cut cap is frame-invariant with Herzog–Huang–Jensen's boundary terms; Re I is frozen along real histories | Rigorous (numerical) | J-6 synthesis §1.7 |
| The rooted tree of histories re-expresses the branch's causal order (Σ as initial boundary) and the state's record arrow; it derives neither; root uniqueness is a statement about dominance | Sketch, by citation and on SCB's fixed-background results | J-6 note §5 |
| The junction constrains any *other* coupling, or selects a value rather than a window | Conjecture — open | — |

## Relationship to other programs

- **`signature-change-boundary`** — upstream (`informs`): fixed-background crossing and junction regularity; this program supplies back the smooth-class equivalence of its bounded-expansion-rate condition with K_Σ = 0 (#215 step 2b).
- **`fixed-point-existence`** — upstream (`informs`): the anomaly fixed point; this program supplies back the coefficient-convention reconciliation (#168) and the "large-N, not realistic" correction to `fpe-fixed-point-is-inflationary`.
- **`co-emergence`** — downstream (`informs`): Open Problem 1 (mass), `conj:mass_generation`, and the toy-model side results (#215 step 2a).
- **`gaussian-gravitational-decoherence`** — no relation established.

## Build

Prose only (`notes/`). Port to `index.tex` once J-1 lands; see `AGENTS.md` for the convention.
