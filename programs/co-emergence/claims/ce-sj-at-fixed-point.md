---
id: ce-sj-at-fixed-point
program: co-emergence
tier: speculation
status: dead
statement: >
  At an exact fixed point of the Level-2 self-consistency map, the
  Sorkin-Johnston construction yields a positive-semidefinite two-point function
  satisfying the CCR, so ce-nohilbert's obstruction does not bite at the
  self-consistency locus, where the metric is the map's output rather than an
  input.
hypotheses:
  - g* an exact fixed point of the semiclassical Einstein equation, a2 > 0
  - free conformally coupled scalar
falsifier: >
  Either W_SJ fails positivity on g*, or positivity requires a choice not
  derivable from g* alone.
consequence: would narrow ce-nohilbert to off-fixed-point formulations
provenance: {born: 2026-07-25, born_by: generator}
disposition:
  outcome: killed
  date: 2026-07-25
  cause: >
    The stipulated hypothesis class is the documented failure case. Aslanbeigi &
    Buck (JHEP 08 (2013) 039), verified verbatim from the PDF: conformal coupling
    forces nu = 1/2 "in every spacetime dimension", and "the SJ vacuum for the
    conformally coupled massless scalar field corresponds to the Euclidean state
    in odd dimensions, and is ill-defined in even dimensions". D = 4 is even. On
    the Poincare patch, which is the paper's actual slicing, the prescription is
    undefined as eta_max -> 0 because the Bogoliubov coefficients diverge. This
    is stronger than falsifier (b) anticipated: not a hidden choice, outright
    non-existence. Conformal coupling is not detunable here - it is definitional
    to the a2 anomaly mechanism.
  defense_attempted: yes
  defense_findings: >
    The defense CORRECTED the attack on one point and then killed the
    speculation on better grounds. The attack's "sequential Step 1" objection
    proves too much: taken to its conclusion it indicts every fixed-point
    existence proof, including this program's own Banach argument built on
    Pinamonti-Siemssen. Refined, it is vehicle-specific and reinforces the
    Aslanbeigi-Buck finding rather than standing alone.
    The Brum-Fredenhagen softened-SJ rescue was checked at source and fails
    cleanly: Fewster & Verch record that it "involves a dependence on a smoothing
    function as an extra parameter", which is exactly the choice falsifier (b)
    predicted, plus the modified states "are unlikely to carry the
    interpretation of vacuum states", plus no confirmed de Sitter application.
  independently_fatal: >
    Horn A/B, and it does not depend on the state-construction question at all.
    The exact eternal solution is where the SJ failure bites; the physically real
    finite-lived phase is not a fixed point of F. Even a total win on state
    construction leaves the claim about an idealised, never-realised point.
---

Dead, but the defense found the vehicle was the problem rather than the thesis -
see [[ce-euclidean-vacuum-at-fixed-point]] for the reframing, which survives the
Aslanbeigi-Buck finding entirely and is gated on the Horn A/B caveat instead.
