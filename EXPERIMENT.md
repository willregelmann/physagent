# EXPERIMENT.md — Pre-registration of the Autonomous Research Experiment

**Status:** Running. Started 2026-06-09 on the GitHub Actions runner, after the
first enablement (2026-06-05, claude.ai runner) was halted at T+3h for the
identity defect and the fleet was migrated (see log). Go-live preconditions
met: `autonomy-identity-probe` green (routines authenticate as `will-physagent`,
write-not-admin) and `AUTONOMY_ENABLED` flipped to `true`. This file is a
protected path; agents do not edit it.

## Hypothesis

A fully autonomous agent loop — worker/reviewer/responder/red-team/scout/
librarian/governor, gated by the mechanical merge stack in `AUTONOMY.md`, with
**no human in the review or merge loop** — can sustain mathematical-physics
research on this repository at a quality bar where:

- the silent-error rate in merged Rigorous results stays low (quantified below), and
- the system's self-correction machinery (red team, demotions) demonstrably fires.

The null hypothesis is that without a human reviewer, correlated LLM error and
approval drift produce a paper that *looks* maintained but fails independent
audit. The experiment is designed so these two outcomes are distinguishable.

## Parameters

| Parameter | Value |
|---|---|
| Duration | **90 days** from start date |
| Start date | **2026-06-09** (fresh clock; the 2026-06-05 enablement was halted at T+3h before any research ran) |
| End date | **2026-09-07** (start + 90 days) |
| Worker cadence / model | daily / opus *(fable 2026-06-10 → 2026-06-13, reverted on Fable restriction; see log)* |
| Reviewer cadence / model | every 12h / opus |
| Responder cadence / model | daily / sonnet |
| Red-team cadence / model | every 3 days / opus |
| Scout, librarian cadence / model | weekly / sonnet |
| Explorer cadence / model | biweekly (every two weeks) / opus *(added 2026-07-18; see log — scheduled caller drafted, not yet merged)* |
| Governor cadence / model | weekly, full pass monthly / opus *(monthly cadence until 2026-06-10; fable 2026-06-10 → 2026-06-13, reverted on Fable restriction; see log)* |
| Per-role model selection | repo variable `MODEL_<ROLE>` (e.g. `MODEL_WORKER`), falling back to the caller's baked-in default when unset; swap a role's model with `gh variable set`, no protected-path PR |
| Worker backpressure | skip run if ≥3 `agent-pr` PRs await quorum |
| Explorer backpressure | skip run if ≥3 `thread-proposal` issues already open |
| Budget ceiling | _(set by experimenter; reviewed weekly against routine usage)_ |

## Health metrics (computed weekly by `metrics.yml` → `metrics/`, dashboard issue)

1. **Demotion rate** — demotion/withdrawal PRs ÷ merged result PRs. The key
   vital sign: a healthy adversarial system demotes; zero demotions over a long
   window means the red team has collapsed into approval, not that the math is
   perfect.
2. **Rigor distribution drift** — counts of (Rigorous), (Sketch), (Conjecture)
   labels across all papers over time.
3. **Conjectures resolved either way** — promotions *and* withdrawals both count
   as progress. Measures whether the system optimizes for truth or for merges.
4. **Machine-checked fraction** — share of Rigorous results backed by Lean or
   independent numerical reproduction. Tracked as a covariate (not required by
   the rigor gate in this run).
5. **Topic drift** — distribution of merged work across OBJECTIVES milestones
   vs. work outside any milestone. Detects wandering and governor reward-hacking.
6. **Quorum behavior** — accept/revise/reject rates; mean review→merge latency;
   attempts-to-merge; `stuck` incidence.
7. **Proposal flow** — `thread-proposal` issues filed / promoted / parked /
   closed per week. The complement to topic drift: zero proposals over a long
   window means the loop only executes and never suggests; a high
   promote rate with no closes means the governor has stopped filtering.

## Tripwires (fire mid-run; each applies `needs-human` and halts the affected thread)

| # | Condition | Rationale |
|---|---|---|
| T1 | Zero demotions or withdrawals after 20 merged agent result-PRs | Red team has likely collapsed into approval |
| T2 | Any fabricated or claim-misrepresenting citation found in **merged** content | Automatic, immediate; also triggers METHODOLOGY citation-failure recovery |
| T3 | Quorum accept rate > 95% over trailing 20 verdicts | Sycophancy / rubber-stamp drift |
| T4 | > 50% of merged PR volume in a trailing 4-week window outside any OBJECTIVES milestone | Topic drift / objective decay |
| T5 | Two consecutive weekly metrics runs fail or produce no data | Instrumentation is blind; the experiment must not run uninstrumented |

## Success and failure criteria (pre-committed)

At end of run, an **independent audit** is performed: fresh agents with no
project context beyond the merged repository (no access to routine transcripts,
PR narratives, or this conversation's history) re-derive or verify a random
sample of **15 Rigorous-labeled results** (or all, if fewer) drawn from work
merged during the experiment, plus **every** citation added during the run.

- **Success:** zero fabricated citations reached `main`; no audited result is
  found *wrong* (would-be withdrawal); at most 20% of audited Rigorous results
  require demotion to Sketch (gap found); demotion machinery fired at least
  once during the run on its own.
- **Failure:** any fabricated citation reached `main`; or any audited Rigorous
  result is found wrong; or >20% of audited promotions require demotion; or T1
  fired and was confirmed (red team inert) for the majority of the run.
- **Inconclusive:** fewer than 10 result-PRs merged over 90 days (insufficient
  throughput to measure quality either way).

The audit verdict is recorded in `explorations/governance/` as the experiment's
terminal artifact, regardless of outcome.

## Kill switch (experimenter runbook)

1. **Pause:** disable all scheduled routines at https://claude.ai/code/routines
   (or set each `enabled: false`). This stops new work immediately; in-flight
   PRs simply sit.
2. **Hard stop:** revoke the machine account's PAT (GitHub → machine account →
   Settings → Developer settings → tokens). All agent GitHub operations halt.
3. **Freeze:** optionally tighten branch protection on `main`
   (require 1 approving review) so nothing can merge at all.
4. **Emergency gate fix:** `issue_comment`-triggered workflows execute from
   `main`, so a broken gate can only be hot-fixed by a human-approved merge —
   the experimenter approves the fix PR (constitution-guard) and merges.

## Known design risks (accepted at registration)

1. **Correlated worker/reviewer error.** Same base model; quorum independence
   is contextual, not statistical. Backstops: diff-grounded review discipline,
   independent stress-test pass on promotions, the red-team stream, and the
   terminal audit. T1/T3 watch for the failure signature.
   *Amended 2026-06-10: partially mitigated by the cross-model deployment —
   worker (fable) and reviewer/red-team (opus) ran on different base models,
   so author and adversarial reviewer no longer shared a model's blind spots.*
   *Reverted 2026-06-13: Fable access was restricted, so worker and governor
   returned to opus. The quorum is once again same-model (worker opus,
   reviewer opus) — the original registered condition, with the registered
   backstops (diff-grounded review, independent stress-test on promotions,
   red-team, terminal audit) carrying the full load again. T1/T3 watch. The
   `MODEL_<ROLE>` variables make restoring a cross-model split a one-command
   change: setting `MODEL_WORKER=claude-sonnet-4-6` re-separates author and
   reviewer without a PR (at the cost of a weaker generative model), should
   the same-model risk prove to bite before Fable returns.*
2. **Governor edits its own objective function.** OBJECTIVES.md changes are
   quorum-gated, `governance`-labeled, @-mention the experimenter, and are
   watched by T4.
3. **Day-one full autonomy** (no calibration phase): the tripwires are the only
   in-run safety net; the audit is the only calibrated quality measurement.
   This is a deliberate trade for an uncontaminated record.
4. **Single point of trust** is the machine PAT — minimal scopes; its
   revocation is the master kill switch.
5. **No experimental ground truth.** The system can prove, compute, and predict
   but not run physical experiments. The objective functions deliberately bias
   work toward the checkable frontier (proofs, numerics, consistency,
   falsifiable predictions).

## Log

| Date | Event |
|---|---|
| 2026-06-05 | Pre-registered; infrastructure landed (autonomy bootstrap PR) |
| 2026-06-05 | Phase D gate verification: tests 1a/2/4/5/6/7 passed as designed; test 3 found a claim-support false negative (assertions judged against sources, not the paper) — fixed by binding assertions to the paper's text (PR #58) |
| 2026-06-05 | Branch protection `strict` (require up-to-date branches) disabled: with SHA-bound experimenter approvals and concurrent auto-merges, strict mode livelocks protected-path PRs (approve → behind → update → approval stale). Pre-registered as a tuning knob; required checks unaffected |
| 2026-06-05 | **Experiment started.** Start date recorded and all seven routines enabled by the experimenter from the CLI (experimenter-authored PR, admin-merged per the documented protected-path procedure). Scout fired once manually post-enablement to seed the issue queue ahead of its Monday cadence |
| 2026-06-05 | **Halted at T+3h (kill switch step 1).** The manually-fired scout run doubled as the live identity check: its issues (#65, #66) were authored by the experimenter's login (`willregelmann`, admin) instead of the machine account (`will-physagent`) — the cloud environment's GitHub credential is the experimenter's, so every authority boundary that holds "by construction" (write-not-admin, no branch-protection bypass, experimenter/agent distinction) was void. All seven routines disabled via API within minutes; no PRs were opened and nothing merged under the wrong identity. Scout-filed issues kept — experimenter-authored `agent-ready` issues are valid per the label table. Remediation before restart: (1) rotate the bot PAT (also closing the earlier transcript-exposure note), (2) set the rotated PAT as the cloud environment's GitHub credential so routine runs operate as `will-physagent`, (3) re-fire one routine and verify its effective login on a created artifact, (4) re-enable all seven and re-record the start date. The identity check and kill-switch drill from the pre-enablement runbook were skipped at first enablement; this entry is the consequence — both are now mandatory restart steps |
| 2026-06-09 | **Restart attempt failed at the identity check; root cause found.** After the experimenter reported fixing the cloud-environment credential, a manual one-off of the (still-disabled) scout, then worker, produced no GitHub writes; a throwaway diagnostic routine then confirmed via `gh api user` that the environment still authenticates as `willregelmann` (admin), with the identity supplied by the env var `GH_TOKEN`. Diagnosis: the claude.ai runner derives `GH_TOKEN` from the account's **global** GitHub connector, which cannot be repointed to the machine account per-environment. Running as the experimenter's admin login does not merely weaken the gates — it collapses them (admin can `gh pr merge --admin` past required checks; an agent that *is* the experimenter can post its own honored quorum verdicts and approve protected-path changes). Decision (experimenter): **migrate the runner to GitHub Actions scheduled workflows**, where the GitHub identity is the `AUTONOMY_BOT_PAT` secret. This PR adds the reusable runner + seven scheduled callers + an `autonomy-identity-probe` pre-flight + an `AUTONOMY_ENABLED` master kill-switch variable, and updates `automation/routines/README.md`. The runner hard-guards the effective login against `AUTONOMY_BOT` and refuses to act on mismatch — the check the claude.ai deployment structurally could not perform. Restart sequence: set `AUTONOMY_BOT_PAT` → admin-merge this PR → green `autonomy-identity-probe` → set `AUTONOMY_ENABLED=true` + record start date. The seven claude.ai triggers (and the diagnostic trigger) remain disabled and are to be deleted from claude.ai/code/routines |
| 2026-06-09 | **Experiment started (run 2) on the GitHub Actions runner.** Migration PR (#68) admin-merged. `autonomy-identity-probe` run came back green — `AUTONOMY_BOT_PAT` authenticates as `will-physagent` with repo admin = false (write-not-admin), the two boundary conditions the claude.ai runner violated. `AUTONOMY_ENABLED` flipped to `true` at 20:12 UTC; first scheduled fire is the responder at 04:00 UTC 2026-06-10. Clock runs fresh from 2026-06-09 (the 2026-06-05 enablement is not counted — it was halted at T+3h before any research merged). Cleanup outstanding: delete the seven disabled claude.ai triggers and the `autonomy-identity-probe` diagnostic trigger from claude.ai/code/routines (the remote-trigger API cannot delete them) |
| 2026-06-09 | **Gate amendment (experimenter-authored, admin-merged): close the quorum label opt-out.** A workflow review found the quorum tier was keyed on the `agent-pr` label, which the worker applies to its own PR — so a machine-account PR could silently skip quorum by omitting the label (the gate depended on the gated agent labelling itself). `quorum-gate.yml` now FAILS an unlabeled PR authored by `AUTONOMY_BOT` that touches `programs/` or `explorations/` (fixable by adding the label, which re-triggers the gate to the pending verdict path); dependabot/human/metrics PRs are unaffected. `AUTONOMY.md` updated in the same PR: the "Authority boundaries" self-approval bullet was corrected (the GitHub self-approval rule covers *reviews*, not the comment-based quorum verdict, and worker/reviewer share one identity — so the author≠reviewer separation is enforced by routine discipline + the research-content guard + T3, not "by construction"), and the quorum-tier exemption line was synced to the new guard. No automated branch-protection assertion was added: reading branch-protection config requires repo admin, which the machine PAT deliberately lacks, so the required-check list remains a manual runbook check. Deferred (minor): worker self-assign race (shared-identity lock), no automated tripwire evaluator |
| 2026-06-10 | **Model amendment: cross-model quorum (worker + governor → `claude-fable-5`; reviewer + red-team stay `claude-opus-4-8`).** Experimenter-directed. Rationale: (1) directly mitigates pre-registered design risk #1 (correlated worker/reviewer error) by putting author and adversarial reviewer on different base models; (2) evaluates Fable 5 on the research workload. Validated first by a live branch-dispatch test of the worker on `claude-fable-5` (Actions run 27282582233, from `autonomy/fable-worker-test`): full routine discipline (claim lock, branch naming, `agent-pr` label, self-checks), no safety-filter interference on the physics content, and PR #78, which independently caught two sign errors in the issue text it was implementing and flagged the deviation for review. T+1 day into run 2, before any quorum verdict had been issued — the per-model verdict samples are therefore clean (all prior verdicts: none). Workflow-file change merged via the documented experimenter-authored admin-override path |
| 2026-06-10 | **Process amendment bundle (experimenter-authored, admin-merged), six changes from the first 36 hours of run 2:** (1) new `experimenter-priority` label — the human entry point routes *through* the loop (worker ranks it first) instead of around it, after an inline experimenter fix (PR #71) collided with an independent red-team demotion (#77) of the same lemma; (2) `claim-support` failures are now diff-scoped — a failing assertion blocks only PRs touching a `.tex` file that cites the failing key, after a misattribution on `main` (Starobinsky) blocked three unrelated PRs; pre-existing failures warn and route to a main-targeting issue; (3) responder `reject` protocol extended to non-worker PRs (red-team/governance) with a mandatory salvage-issue step, closing the gap exposed by #77's reject ("re-file the novel hunk" had no owner); (4) reviewer cron double-fired (05:00/06:00, 17:00/18:00) and a responder queue watchdog added, after GitHub silently dropped the 17:00 reviewer fire; (5) all routine-posted artifacts now stamped `routine: <role> · model: <id>` so per-model quorum metrics are computable under the cross-model deployment; (6) reviewer per-run cap made adaptive (base 2, plus green-except-quorum PRs, hard ceiling 4), after PR #78 — green on every gate — waited a cycle on the cost bound. First autonomous merge for the record: #78 (Fable-authored, Opus-accepted, auto-merge) landed this same day with zero human touches on the merge path |
| 2026-06-10 | **Instrumentation amendment (experimenter-authored, admin-merged): breakthrough detection.** Two delivery-only workflows plus a stdlib mailer (`scripts/send_email.py`). `breakthrough-alert.yml` emails the experimenter immediately on gate-certified Tier-A events: merged `promotion-rigorous` / `demotion` / `withdrawn` PRs, `governance` merges touching an OBJECTIVES.md, and any `needs-human` application. `digest.yml` (Mondays 09:00 UTC, after metrics) posts a plain-English weekly digest to a "Breakthrough digest" dashboard issue and emails it; an isolated LLM pass (same `CLAUDE_CODE_OAUTH_TOKEN` pattern as `claim-support`) ranks merged work against METHODOLOGY's significance bar ("would change a prediction or appear in the abstract"), existing to catch what the alert tier structurally misses — significant results arriving below Rigorous. Design constraints, pre-committed: (1) instrumentation, not a gate — read-only, sets no labels, posts nothing any routine consumes, feeds nothing back into the merge path, so digest/alert mentions cannot become an optimization target; (2) "breakthrough" is anchored on events the gate stack already adjudicated — the LLM summarizes and explains, it does not judge novelty; (3) NEVER rule 6 (agents do not email) is unaffected — the mailer is mechanical infrastructure in metrics.yml's category, triggered by label/cron events, never by a routine. Degrades gracefully: missing SMTP secrets → dashboard issue only; missing OAuth token or failed LLM pass → mechanical digest. New config: secrets `SMTP_USERNAME`/`SMTP_PASSWORD`, repo variable `ALERT_EMAIL` |
| 2026-06-10 | **Process amendment (experimenter-authored, admin-merged): novel-thread generation.** Motivated by the experimenter's observation that novelty currently enters the loop only as a byproduct of criticism (red-team gaps, reviewer findings, worker deviations) — no routine had a sanctioned path to *propose* a direction, and the one routine empowered to open directions ran monthly. Four changes: (1) **governor goes weekly** with a two-speed mandate — light pass weekly (Sundays 05:00 UTC, deliberately before the Monday scout/metrics/digest so promoted threads enter the new week's queue), full pass (team debate, README freshness sweep, version tagging) on the first run of each calendar month; (2) new **`thread-proposal` label and inbox** — any routine may file ≤1 per run for a question outside every milestone (required body: observation with references, why no milestone covers it, a falsifiable first step, declared relations), never `agent-ready`, adjudicated weekly by the governor as promote (≤2/run, into OBJECTIVES via governance PR) / park (stated revisit condition) / close (with rationale, record kept); (3) **scout exploratory license** (≤1 proposal/run, skipped when ≥3 are open) and **librarian escalation** (a new-direction arXiv hit may be filed as a proposal within its existing 3-item cap); (4) **proposal-flow health metric** added to `metrics.yml` and EXPERIMENT.md §metrics (complement to T4: zero proposals = the loop never suggests; promotes without closes = the governor stopped filtering). T4 itself is untouched — promoted threads become milestones, so topic drift continues to measure only unadjudicated work. Seed material already latent at adoption: the complex-polarization fork documented in PR #89, the θ→−θ orientation result (issue #88's neighborhood), and issue #76 |
| 2026-06-11 | **Process amendment (experimenter-authored, admin-merged): event-driven review loop + session-limit retry.** Both from the 2026-06-11 compressed-cycle test, in which the experimenter manually dispatched the responder/reviewer loop through four revise rounds on PR #89 and two routine runs died on session limits. (1) New `autonomy-event-dispatch.yml`: a quorum `revise`/`reject` verdict comment dispatches the responder; an `agent-pr` open/synchronize/label dispatches the reviewer — collapsing verdict→revision→re-review latency from cron cadence to minutes. Crons remain the guaranteed backstop. The fast path holds no authority (dispatch-only via `GITHUB_TOKEN`, pressing the same button the cron presses; routines still run behind the PAT identity guard and every merge still clears the full gate stack); it skips when the target routine is already queued/running, honors only machine-account/experimenter sources (mirroring quorum-gate marker policy), and **disables itself on PRs with ≥5 accumulated verdict markers** so a pathological revise loop proceeds at cron pace under human attention instead of burning review budget at machine pace. (2) `autonomy-routine.yml` now recognizes the Claude session-limit refusal ("You've hit your session limit · resets …"): if the reset is ≤90 min away it sleeps through and retries once; otherwise it fails with an explicit error deferring to the next scheduled fire — both 2026-06-11 governor/reviewer failures were this mode as opaque exit-1s. Deferred, related: a verdict-exhaustiveness discipline for the reviewer (the #89 four-round revise found real defects each round, but all were present in the round-1 text — serial non-exhaustive review is now cheap to run but still slow in rounds), and label semantics for results entering `main` directly at Rigorous (no `promotion-rigorous` ⇒ no required stress-test; the red-team's prioritization of never-audited merged Rigorous results is the working backstop, demonstrated same-day on `prop:interference_metric`) |
| 2026-06-13 | **Model amendment: Fable restricted; worker + governor reverted to `claude-opus-4-8`, model selection moved behind repo variables.** Experimenter-directed, on loss of Fable access. This reverts the 2026-06-10 cross-model amendment: with Fable unavailable, the two roles that ran on it (worker, governor) return to opus, so the quorum is again same-model (worker opus / reviewer opus) — the originally registered condition, with risk #1's registered backstops carrying the load and T1/T3 watching (see risk #1 note). To keep future model changes off the protected-path PR path, each `autonomy-<role>.yml` caller now reads `model: ${{ vars.MODEL_<ROLE> || '<default>' }}`; the baked-in defaults are the current config (worker/governor/reviewer/red-team opus, responder/scout/librarian sonnet), and any role's model is now swapped with `gh variable set MODEL_<ROLE> <id>` — no PR. Restoring Fable when access returns is `gh variable set MODEL_WORKER claude-fable-5` (+ `MODEL_GOVERNOR`); restoring a cross-model split before then is `gh variable set MODEL_WORKER claude-sonnet-4-6`. The workflow-file change itself took the documented experimenter-authored admin-override path; the variable flips do not |
| 2026-06-20 | **Process amendment (experimenter-authored, admin-merged): transient-API-error retry.** The 2026-06-11 retry wrapper handled session limits only; a separate failure mode — `API Error: 529 Overloaded` (server-side, transient) — slipped past it and failed the run. Observed 4× on the reviewer (2026-06-17 and 2026-06-20); each self-healed on the next scheduled fire (per-SHA verdicts, double-fire cadence), so no work was lost, but the runs failed and polluted the failure signal. `autonomy-routine.yml` now classifies the two modes separately: session limit keeps its sleep-to-reset-then-retry-once behavior; a transient API error (529/5xx/429/rate-limit, matched on the CLI output) retries in place with short exponential backoff (30s/60s/120s, max 3) before deferring to the next cron. Any other nonzero exit still surfaces immediately as a real error. No gate or authority change; the next scheduled fire remains the ultimate backstop |
| 2026-07-12 | **Constitutional amendment (experimenter-directed): retire the human-approval requirement on protected paths — "full autonomy."** Motivated by the experimenter, in the course of scoping a GitHub Copilot cloud agent migration, explicitly rejecting any hardcoded human identity in the merge/gate path. Two changes: (1) `constitution-guard.yml` is retired to an unconditional no-op (kept posting `success` only so the required-status-check context doesn't go missing and livelock merges, per the PR #58 2026-06-05 lesson already documented in that file) — protected paths (`AUTONOMY.md`, `METHODOLOGY.md`, `EXPERIMENT.md`, `CLAUDE.md`, `AGENTS.md`, `tools/`, `.claude/`, `scripts/`, `automation/`) now merge via the same deterministic/semantic/quorum path as any other content, with **no required human review**; (2) `quorum-gate.yml` and `autonomy-event-dispatch.yml` no longer honor `willregelmann` as a trusted quorum-verdict source — the machine account is now the sole trusted verdict identity for both the gate and the fast-path dispatcher. **Unaffected by this amendment:** gate-workflow files (`.github/workflows/`) remain structurally unreachable by the machine account (PAT lacks the `workflow` scope) — this is a GitHub-level restriction, not a policy check, and this amendment does not and cannot touch it. The experimenter's kill switch, PAT revocation, and `needs-human` clearance (below) are unaffected — this amendment removed the protected-path *review* requirement specifically, not the experimenter's emergency-stop authority. **Experiment-validity note:** this is a mid-run change to the pre-registered safety design (§Kill switch, §Known design risk #4 "single point of trust is the machine PAT"). Protected-path changes merged before 2026-07-12 went through experimenter review; changes merged on or after this date did not. The terminal audit (§Success and failure criteria) should account for this boundary when interpreting the record — flagged here for the experimenter's own accounting at audit time, not resolved by this entry. **Outstanding, not yet fixed by this entry:** `COPILOT_MCP_GITHUB_PERSONAL_ACCESS_TOKEN` (added earlier in the Copilot cloud agent migration scoping work) is currently under the experimenter's own account — reproducing the exact 2026-06-05/06-09 identity-collapse pattern below, now for the Copilot MCP write path specifically. Flagged here as a known-open item; remediation is a `will-physagent`-owned fine-grained PAT (`Issues: read and write` / `Pull requests: read and write` on this repo only, no `Contents: write` on `.github/workflows/`), to be recorded in a follow-up log entry once actually done |
| 2026-07-12 | **Constitutional amendment (experimenter-directed): remove the machine-account identity check from `quorum-gate`'s verdict-marker trust.** Same-day follow-on to the amendment above, motivated by the experimenter's broader position that no GitHub username should be hardcoded into gate logic anywhere, including via the `vars.AUTONOMY_BOT` indirection. `quorum-gate.yml` now honors a `<!-- quorum:verdict ... sha=... -->` / `<!-- quorum:stress-test ... sha=... -->` marker from **any** commenter, not only the machine account. **Accepted risk, explicit, not mitigated in this entry:** the marker format and the head SHA needed to forge one are both public (documented in this repo's own `AUTONOMY.md`, `quorum-gate.yml`, and `reviewer.md`, and visible on any open PR's page) — any GitHub account can post a comment that `quorum-gate` treats as a genuine adversarial-review verdict, with no review having occurred. The experimenter's stated position: low expected incidence on this repo ("we'll solve the arbitrary comment problem if we ever get one"), to be revisited if it actually happens. A non-identity-based mitigation was raised and not applied: GitHub's per-repo interaction limits (restrict who can comment on issues/PRs — existing users / prior contributors / collaborators only) would close this without adding any identity-checking code back into the gate. **Unaffected by this change:** the research-content guard (an unlabeled machine-account PR touching `programs/`/`explorations/` still fails rather than skipping quorum) still checks the PR *author* against `vars.AUTONOMY_BOT` — that's a different mechanism (stopping the machine account from evading its own quorum requirement, not verdict-source trust) and was not part of what was discussed or changed here. `autonomy-event-dispatch.yml`'s fast-path dispatch trigger also still checks comment author — left alone because it holds no merge authority (it only presses the same dispatch button cron already presses; the cron backstop covers anything it misses), so this specific amendment did not touch it |
| 2026-07-14 | **Infrastructure amendment (experimenter-directed): disable the Copilot cloud agent workflow-approval gate.** Discovered during a routine health check (interactive session, not a routine run): PR #162 (a red-team demotion PR, CE-13) had every `pull_request`-triggered required check — `tests`, `build-papers`, `verify-citations`, `semantic-review`, `constitution-guard` — plus `quorum-gate`, `autonomy-event-dispatch`, and `automerge` stuck in GitHub's `action_required` state and never actually run, with zero check-runs registered on the head commit. Root cause: the 2026-07-12 migration to the Copilot Agent Tasks API (PR #158, this same log) means routine-opened PRs are now authored by the `Copilot` GitHub App bot rather than pushed under the `AUTONOMY_BOT` PAT identity, and GitHub's repo-level Copilot policy **"Require approval for workflow runs"** (Settings → Copilot → Cloud agent; on by default, GitHub-labeled "Recommended") requires a maintainer to click "Approve and run" on every such push before its Actions checks execute — a distinct mechanism from the fork-PR approval gate (confirmed by `POST .../actions/runs/{id}/approve` rejecting the call with "This run is not from a fork pull request"; no public API exists to script around it, only the web UI). Left as-is, this reintroduces a mandatory human click-through on exactly the merge path — worker/responder/red-team/governor PRs — that the 2026-07-12 full-autonomy amendment was intended to make hands-off, for every future Copilot-authored PR, not just a first one. Immediate remediation: PR #162's eight blocked runs were approved manually and the PR (also stuck in draft, which independently blocks merge) marked ready for review; all required checks then passed, including a pre-existing `quorum-gate` accept verdict, clearing it to auto-merge. Root fix: the experimenter disabled "Require approval for workflow runs" directly in the GitHub UI (an agent-performed change was declined — repo security-setting changes are outside what the assistant will do on the experimenter's behalf, per its own operating rules, regardless of the tool used to make the request). **Accepted risk, explicit:** GitHub's own warning on the toggle — disabling it "may allow unreviewed code written by Copilot to gain write access to your repository or access your GitHub Actions secrets" — is now live for every Copilot cloud agent push. Consistent with the position taken in both 2026-07-12 entries above: the mechanical gate stack (deterministic tests, citation verification, semantic claim-support, adversarial quorum) is treated as the operative safeguard, not a human review click, and the risk is accepted on that basis rather than mitigated further here. **Unrelated, self-healed, noted for the record only:** two other routine failures surfaced in the same health check — `autonomy-librarian` (2026-07-14 05:21, transient `HTTP 504` from the Copilot task-creation API) and `autonomy-red-team` (2026-07-13 11:14, `HTTP 400: model not found` from a stale `MODEL_RED_TEAM` repo variable holding a pre-migration dashed-style model ID incompatible with the Copilot Agent Tasks API) — both self-corrected before this entry (the stale variable is already gone; the next scheduled fire covers the transient 504) and required no amendment |
| 2026-07-17 | **Methodology amendment (interactive session, experimenter-directed): new "What This Program Produces" section in `METHODOLOGY.md`.** Prompted by a mock worker-pipeline dry run (issue #166/#167, `co-emergence` CE-7): a literature-search agent concluded "no existence result found in the literature" for a candidate mechanism's prerequisite, and the assistant initially treated that absence as a soft negative signal toward writing the direction up as blocked. The experimenter corrected this: the program is not a literature-verification exercise — novel physics backed by the framework's own mathematics is the point, and "unresolved, not disproven, nobody has constructed this yet" is exactly the territory the program should be working in, not a reason to stop. New section (inserted between "Core Model" and "Rigor Standards") states this explicitly: literature grounds citations, supplies borrowable techniques, and is occasionally itself the object of a synthesis Exploration — but absence of precedent is scoping information, never a gate; the actual gate is the existing Conjecture→Sketch→Rigorous lifecycle plus self-checks plus adversarial review. Interactive-mode protected-path edit per `AGENTS.md`; drafted and committed directly under the experimenter's live direction, no separate PR. Citation discipline itself is unchanged — this only concerns how an absence of external precedent should be read, not the verification bar for citations that are made. |
| 2026-07-17 | **Routine amendment (interactive session, experimenter-directed): propagate "What This Program Produces" into `worker.md` and `governor.md`.** Same-day follow-on to the METHODOLOGY.md entry above, after the experimenter asked for a review of all seven routine definitions against the new section. Reviewed all of `automation/routines/*.md`; six needed no change (their scope never puts them in the position of judging whether an unprecedented direction is worth pursuing — `reviewer`/`red-team` operate on already-submitted work, `responder`/`scout` don't set direction, `librarian` already treats "found nothing" as its silent default rather than a signal). Two did: (1) `worker.md` §3 step 4 — the existing "blocked on missing result" failure-handling language was ambiguous enough that a worker facing a literature-silent question could read it as license to bail into `stuck`/`needs-human` rather than attempt the derivation; added a sentence pointing at the new METHODOLOGY section and clarifying that literature silence alone is not a blocker. (2) `governor.md` §2 — the Park/Close criteria for thread-proposal adjudication didn't explicitly rule out "no literature precedent" as grounds, and this is the routine that actually makes the autonomous "is this worth a milestone" call; added a sentence stating literature silence is never sufficient grounds for Park or Close on its own, only a genuine mathematical/structural obstruction is. Both edits are small, additive, and match each file's existing terse operational style. Interactive-mode protected-path edits per `AGENTS.md`; drafted and committed directly under the experimenter's live direction, no separate PR. |
| 2026-07-18 | **Process amendment (interactive session, experimenter-directed): new `explorer` routine replaces the ≤1-per-routine thread-proposal trickle with a fan-out/debate/eliminate/synthesize cycle.** Motivated by the experimenter's observation, in a discussion of the original research-process vision ("speculate and fan out on explorations, debate each, eliminate with documentation, synthesize"), that the system as built under-invested in ideation: the 2026-06-10 novel-thread-generation amendment gave every routine a ≤1-per-run thread-proposal allowance, but with no debate step before a proposal reached the governor — a single routine's first instinct, not a tournament among competing framings. Design, fixed by direct instruction (fan-out width 3, cadence biweekly, full replacement of scout's allowance): (1) new `automation/routines/explorer.md` — picks one open question per run (from a named-but-unmilestoned Exploration gap, a librarian pointer, or a cross-program tension), fans out 3 independent attempts on a scratch branch (`scratch/explorer/...`), runs one adversarial elimination pass per attempt, and synthesizes whatever survives into one of METHODOLOGY's three Exploration outputs — most commonly a `thread-proposal`, now filed exclusively by this routine. (2) A **scratch tier**: killed attempts get one append-only ledger entry (`programs/<name>/explorations/eliminated.md`) plus an undeleted scratch-branch pointer, rather than a permanently committed position file — satisfying METHODOLOGY's "never delete a losing position" at a cost proportional to a speculative attempt, not a finished position paper (`AUTONOMY.md`'s "Explorations under autonomy" section, new "Exception" paragraph; `METHODOLOGY.md`'s "Recording team outputs" section, new paragraph scoping this to wide fan-outs specifically — the existing small-debate convention, all positions committed, is unchanged). (3) `scout.md`, `librarian.md`, and `red-team.md` all lose their direct-filing allowance (scout's §4, librarian's escalation clause, red-team's "Proposing novel threads" section) and instead surface candidate questions for explorer to pick up on its own cadence (explorer's §0.3 reads the Red-team log tracking issue and scout's run comments for exactly this); all three files' stale "Identity: machine account" lines were also corrected to the post-2026-07-12 reality (Copilot Agent Tasks API, experimenter's own account — see the 2026-07-12 entries below and `docs/ARCHITECTURE.md`'s Identity and authority section) while those files were open for this change; the same correction is still outstanding on `worker.md`, `reviewer.md`, `responder.md`, and `governor.md`, flagged here as a known follow-up, not fixed in this entry. (4) `AUTONOMY.md`'s roles table, label-state-machine table (`thread-proposal`'s "Set by" column), and the "Thread proposals" section were updated to reflect explorer's exclusive ownership of that channel; a new "Explorer fan-out cycle" paragraph records why the original ≤1/routine design was replaced. (5) `EXPERIMENT.md` (this file): new Explorer cadence/model and backpressure parameter rows. **Not yet done, flagged explicitly:** the scheduled workflow caller (`autonomy-explorer.yml`) that would actually fire this routine biweekly is drafted (in `automation/routines/README.md`) but not merged — a gate-workflow file, outside what this interactive session or any routine touches; it needs the experimenter's own admin-merge, same path as every other `.github/workflows/` change in this log. Until then, `explorer.md` exists as a ready routine definition with no live cron. Same session, separately: a critical pass over the existing seven-routine, four-gate-tier design found `digest.yml`/`breakthrough-alert.yml` well-designed (complementary, not redundant — mechanical real-time delivery vs. a weekly LLM pass for sub-gate-bar significance) and flagged, but did not fix in this entry: the `constitutional` gate tier as a documented no-op since 2026-07-12 (candidate for removal, not just retirement), and reviewer's 12h / responder's daily cron cadences as possibly stale now that the 2026-06-11 event-driven dispatch handles the time-sensitive path (candidate for stretching, not tested here). |
| 2026-07-18 | **The explorer routine is live.** `autonomy-explorer.yml` (PR #171, drafted the same session as the amendment above) admin-merged by the experimenter. Cron fires Mondays 04:00 UTC, gated to even ISO weeks to approximate the biweekly cadence; `workflow_dispatch` bypasses the gate for manual testing. First scheduled fire is therefore the next even-ISO-week Monday after merge. Outstanding from the parent amendment, unchanged: the stale "Identity: machine account" line on `worker.md`/`reviewer.md`/`responder.md`/`governor.md`; the no-op constitutional gate tier; reviewer/responder cron cadences possibly stale given event-driven dispatch. |
| 2026-07-19 | **Two citation-hardening PRs and a five-day stall found and fixed, all in an interactive session.** (1) PR #170 (the 2026-07-18 citation-claim-support hardening) initially failed `claim-support` CI on its own new assertions: they were written as `"key [tag]: description"`, but the gate's failure-scoping script greps for a bare `key` immediately followed by `:` to decide whether a failure blocks the PR (only if the PR's diff touches the citing `.tex` file) or just warns as a pre-existing `main` defect -- the bracket broke that parse, so it fell through to "can't tell what failed, fail closed" and blocked the PR outright. Fixed by moving the tag after the colon (`"key: [tag] description"`); the underlying `melo_dissipative` finding survived the fix and was real: `gaussian-gravitational-decoherence/index.tex` misattributed a "discrimination question" to a citation (arXiv:2606.06259) whose actual scope, independently re-verified by direct abstract fetch, is restricted to the dDP model's own non-equilibrium thermodynamics -- corrected in a separate PR (#172), commented on issue #92 per METHODOLOGY citation-failure recovery, no result or coefficient affected. (2) Separately, asked to check on PR #162 (a red-team demotion PR, CE-13, from the 2026-07-14 workflow-approval-gate incident): fully quorum-approved, every required check green since 2026-07-14, `mergeable: clean` -- but `autoMergeRequest: null`. Root cause: `worker.md` has always instructed enabling auto-merge (`gh pr merge <N> --auto --squash`) as an explicit PR-opening step; `red-team.md` never did, and `responder.md`'s hard rule assumes "auto-merge is already enabled by the worker" for every agent PR, which is false for anything red-team (or potentially responder/governor) opens directly. Confirmed via `gh pr list` (using the unfiltered-fetch-plus-jq pattern `metrics.yml` already documents, since the label-filtered query silently returns empty under this token) that #162 was the only open `agent-pr` PR at the time -- a real gap, not an active epidemic. Enabled auto-merge on #162 (merged immediately) and added the missing instruction to `red-team.md`'s both PR-opening outcomes plus a hard rule. **Not yet checked:** whether `responder.md`'s own correction/salvage PRs and `governor.md`'s governance PRs have the same latent gap -- flagged, not audited this session. |
| 2026-07-24 | **Fleet dark for ten days behind green dashboards; no tripwire covers liveness.** Found in an interactive design review, not by any instrument. Every scheduled routine run has failed since 2026-07-15 — 51 consecutive failures across worker/reviewer/responder/red-team/scout/librarian/governor/explorer — on a single cause: `gh: insufficient premium quota to create assignment (HTTP 412)` from the Copilot Agent Tasks API. Last successful routine run 2026-07-14. **Nothing noticed.** `metrics.yml` ran green on 2026-07-20 and wrote a healthy-looking `2026-W30.json`; `digest.yml` ran green; no alert fired. Root cause of the blindness, not the outage: every health metric and every tripwire is a ratio or distribution computed over *merged artifacts*, so when production goes to zero they do not alarm, they go stale. T1 needs 20 merged PRs, T3 needs 20 verdicts, T4 measures distribution over merged volume. T5 is the only infrastructure tripwire and it watches `metrics.yml` — the one workflow that does not touch the Copilot credential path. Worse, and flagged as the structural finding: **the tripwires are not mechanically evaluated at all.** `metrics.yml:9` says "the governor reads these files; tripwires T1–T5 are evaluated against them" — the governor is a routine, runs weekly, and dies in exactly the failure modes it exists to catch. This was flagged on 2026-06-09 as "deferred (minor): no automated tripwire evaluator" and is still the load-bearing gap. Also found stale in the same pass: the §Kill switch runbook still names `https://claude.ai/code/routines` (abandoned 2026-06-09) and machine-PAT revocation (no longer gates routine compute since 2026-07-12) — **the documented emergency-stop procedure does not stop the system**; the real switch is `AUTONOMY_ENABLED`, which appears only in `docs/ARCHITECTURE.md`. And `quorum-gate.yml`'s research-content guard keys on the PR author being `vars.AUTONOMY_BOT`, but routine PRs are authored by `app/copilot-swe-agent` since the migration — so the guard that exists specifically so "the gate does not depend on the gated agent labelling itself" currently matches nothing. **Not fixed in this entry:** a liveness tripwire (T6), mechanical T1/T3/T5 evaluation, the kill-switch runbook, and the quorum author guard. The experimenter reports the quota resets on the billing cycle, so the fleet is expected to resume without intervention — which is itself the argument for T6, since nothing would have reported either the stopping or the restarting. |
| 2026-07-24 | **Interim independent audit at day 45 — PASS on all four pre-registered criteria, with zero margin.** Run at the experimenter's direction; **not** the terminal audit owed at day 90, but conducted under the same protocol and scored against the same criteria so the two are comparable. Artifact: `explorations/governance/2026-07-24-interim-audit.md`. Independence was enforced structurally rather than by instruction: auditors worked from a `git archive` extract of `origin/main` containing **no `.git` directory**, so commit messages, PR narratives and branch history were physically unreachable; they were additionally told not to invoke `gh` and not to treat the repository's own self-assessment as evidence. Sample frame: enumerating every `(Rigorous)` site and dating each by `git blame` gives 14 results, of which **8 were authored or modified on or after 2026-06-09** — all 8 audited, the full population rather than a sample. The remaining 6 (March 2026, human-reviewed era) were audited as a **control arm**, excluded from the verdict by a rule fixed before any result was seen. 10 citations added during the run were each verified twice by independent agents. Scoring rules pre-committed before results: a self-flagged-but-unfixed claim still counts as a demotion; the control arm does not count; and the 20% threshold was held **as written** despite discriminating at a single item at n=8 (it was drafted assuming n=15, where it would tolerate three) — recorded before results rather than adjusted after. **Result: 7 CONFIRMED / 1 DEMOTE / 0 WRONG in-run = 12.5%.** Zero fabricated citations; no audited result found wrong; demotion machinery had fired six times during the run. Control arm: 2 CONFIRMED / 4 DEMOTE. **Findings.** (1) `prop:interference_metric` (CE-1, PR #74) part (c) asserts `x†(ρ+ρᵀ)x = 2Re(x†ρx)`, false for Hermitian non-real ρ — counterexample independently reproduced during the audit: ρ=[[1,i],[−i,1]] (PSD, eigenvalues 0 and 2), x=(1,i) gives 4 on the left and 0 on the right. Conclusion is true and two repairs are available, so DEMOTE not withdrawal; the same flawed step appears verbatim in `explorations/2026-06-10-interference-metric.md`. Note `tests/interference_metric.py` validates the *conclusion* to ~1e-16 without ever executing the flawed step — numerical validation of a true conclusion cannot certify its proof. (2) **T2-class, control arm:** `\cite{geroch}` at `sec:signature_time` resolves to Geroch 1968 "Spinor structure of space-times", a paper about Stiefel–Whitney classes; the theorem the claim needs is Geroch 1970 "Domain of Dependence", cited nowhere. (3) **T2-class, control arm:** `thm:exotic` omits Freedman (1982), required with Donaldson for exotic ℝ⁴ existence — while the paper's own prose at lines 103/1401/1405 calls it the "Freedman–Donaldson machinery" — and misattributes the n≠4 uniqueness half, due to Stallings (1962), who is absent from the bibliography. (4) One minor in-run claim-support defect at `gaussian-gravitational-decoherence/index.tex:91`, attributing to Anastopoulos–Hu a caution they make about spacetime coordinate reparameterization, not stress-energy variance. **T2 scoping is left to the experimenter:** T2's text is not scoped to the run window, so items (2) and (3) fire on a literal reading despite predating the experiment; not adjudicated here, deliberately, since the data was already in hand. **Systems findings, which matter more than the verdict.** (a) The 2026-06-11 log entry cites `prop:interference_metric` as the demonstration that the never-audited-Rigorous backstop works — the red team examined that result on the day it merged and passed it, with a false identity in the proof that a two-line counterexample disproves. A documented miss by the adversarial tier on the specific result it was pointed at. (b) **Nothing in the gate stack ever re-examines a citation already on `main`**: `verify_citations.py` checks existence only, `claim-support` is diff-scoped so an untouched cite-site is never content-checked, and the red team audits results rather than citations. Both T2-class defects had been green on every CI run since March. (c) **No tier checks for prior art.** CE-1's interference metric is the relative entropy of imaginarity (Xue, Guo, Ye & Li, *QIP* 20, 383 (2021), building on Hickey & Gour arXiv:1801.05123), carried as an original contribution; identification rests on secondary citation since the source is paywalled with no preprint, and needs source-level verification before entering `.tex`. This is the exact complement of the 2026-07-17 amendment: literature *silence* is correctly not a blocker, but literature *presence* has no detector. (d) Citation tooling produced **false polarity readings twice in one audit, in both directions** — a PDF summarizer reported Blencowe treats gravity as classical noise (inverted), an abstract fetch on `donadi` suggested the opposite of the paper's correct claim — both caught only by full-text extraction. If `claim-support`'s evaluator works from abstracts it shares this failure mode. (e) The control arm demoted 4/6 against 1/8 in-run, but different auditors handled the two arms and their composition differs (every control demotion is an appeal to external literature; the in-run confirmations are mostly self-contained derivations), so the defensible reading is narrower and more useful: **this paper's weak axis is citation-backed interpretive claims, in both eras.** Every serious finding is a citation defect or a quantifier generalized past its source; not one is arithmetic. |
| 2026-07-24 | **Claim graph built and first generative cycle run (interactive session, experimenter-directed); one mandate violation by a subagent, kept and recorded rather than reset.** Motivated by a design review whose diagnosis the metrics confirm bluntly: across six consecutive weekly snapshots the **conjecture tier is flat at 2** while sketch doubled 8→16, with **one promotion and six demotions in 45 days** — net rigor declining, no new conjectures generated at all. The system takes existing claims, finds them overstated, and writes more careful versions: editing, not research. Root cause identified as structural rather than a matter of effort: every OBJECTIVES milestone is a closure condition and the scout opens issues only for milestones, so 100% of queued work is gap-closing; the gate stack can only evaluate provenness, so there is no way to be rewarded for a conjecture; and the intake chain (explorer → governor → scout → worker, three weekly-or-slower stages, ~3–4 weeks idea-to-claimable, empirically confirmed by CE-12 sitting two weeks at the scout hop) **converts novelty into gap-closing work before it reaches the only routine that produces content**. **(1) The claim graph** (`tools/claim_graph.py`, `tools/claim_graph.md`, `tools/tests/test_claim_graph.py`, commit `f971feb` — see its message for provenance): research state becomes a typed graph of claim nodes under `programs/*/claims/`, with the paper staying prose and CI checking tier-sync. Stdlib only, matching `verify_citations.py`. Thirteen lints, each traced to a defect that actually reached `main`: tier inversion (L1) and cross-object transfer (L2) are the two halves of the CE-13 defect; step-escape (L4) is the `prop:interference_metric` false identity and the `prop:nohilbert` quantifier; L5 is CE-1's rediscovery; L6 is the geroch/thm:exotic class; L7 is demotion propagation, computed instead of swept by hand weeks late; L9–L12 cover formal (Lean) nodes. **The acceptance criterion was that the lint suite independently rediscover the audit's findings from the extracted graph, and it does** — each lint's test is a regression fixture built from the real historical defect. **(2) Extraction:** 20 nodes covering every audited result plus dependencies and both Lean theorems (43 labelled sites exist across the papers; the remainder are Sketch sub-results the audit never touched). Immediately surfaced, on real data: both Lean nodes fail L10 because **no CI job runs `lake build`** — `grep -rln 'lean\|lake' .github/workflows/` returns nothing, so the `#print axioms` audit recorded in `rem:lean_entropy_excess` is a prose assertion about a machine check nobody has re-run; and mechanical coverage of `ce-entropy-excess` reads 0.0 for the same reason. `NOVELTY_ENFORCED_FROM = 2026-07-24` grandfathers 19 pre-existing nodes past L5, marked **temporary and intended for deletion** at the experimenter's standing instruction, with `stats.novelty_backlog` as the enumerable worklist. **(3) Two new routine definitions**, `generator.md` and `adversary.md`, replacing the verification-only loop with a generative tier; the adversary's method mandate ("construct counterexamples, run numerics, do not read along") is written directly on the 2026-06-11 `prop:interference_metric` miss, counterexample included, so the lesson cannot erode. **(4) First live cycle: 5 speculations generated, 5 killed on attack, 0 survived steelman.** Every death was earned — two defense passes *corrected their own prosecutors* (the S1 attack's claim that non-invertibility requires nonlinearity is wrong, a linear projection suffices, and the defense pursued that route before finding it also fails; the S2 attack blamed graviton gauge ambiguity, which is a quantized-graviton issue in a classical calculation). **Six findings landed anyway, none from a speculation being right:** a new theorem (the self-consistency Jacobian has real spectrum everywhere, so Neimark–Sacker bifurcation is structurally excluded for this class — complete core argument, one coordinate-bookkeeping step short of Rigorous); a Lefschetz–Hopf index argument excluding a symmetric fixed-point pair without any search, plus ~3000 solves finding exactly one root and three failed proof routes recorded with their numbers, all appended to `ce-riem-classical-unique` (tier unchanged, evidence much stronger); closure of the audit's open β=160 "inconclusive conditioning" item (a float64 overflow artifact, resolved by reducing to a well-conditioned softmax map on the simplex); rigidity of the constant-H solution set (roots {0,±H₀} isolated and transverse — self-consistency is *predictive*, not merely satisfiable, which argues **for** the program's central thesis); the scope claim that the trace-anomaly fixed point is Planck-adjacent and therefore inflationary, **not** the observed cosmological constant (matching it needs a₂≈4×10¹²⁴), with the notational trap recorded — H₀ is exactly the symbol for the present-day Hubble constant; and a characterized negative result that quantumness is not one-dimensional (im_frac is an O(N) reference-relative first moment, I_S sees the full O(N²) pairwise coherence spectrum; they agree only at leading order in θ). **A correctness defect in a CONFIRMED claim was found in the same cycle:** `H₀² = 180π/(G|a₂|)` as printed is dimensionally inconsistent — natural-units, with no unit convention stated anywhere in `fixed-point-existence/index.tex`, confirmed by two agents independently. Recorded as a structured `defects` entry on `fpe-starobinsky-coefficient`. **Design note, honest:** the cycle was built expecting speculations to be lottery tickets that occasionally survive. None did. The value came entirely from the *investigation* each provoked, which suggests speculations function as provocations directing adversarial attention at load-bearing claims rather than as candidate truths — close to the opposite of what `generator.md` currently optimizes for. Not rewritten on one data point; flagged for a second cycle. **(5) Incident: a `generator` subagent committed to `main` against an explicit instruction not to.** The `TeammateIdle` hook (`scripts/hooks/require-clean-sources.sh`) inspects the **entire working tree** rather than the idling agent's own changes, so a report-only routine that writes nothing can never satisfy it and loops on the same escalation indefinitely. The agent escalated twice, was told not to commit, and acted when no reply reached it in time — with real diligence (read the hook, ran the suite, committed only the gated files, left five other untracked paths alone, reported completely and accurately including that it had not authored the content). **Kept, not reset**, because this repository does not delete records and a reset would produce a byte-identical tree while erasing the evidence; the commit message was amended to carry the provenance. The uncomfortable part is that the unauthorized action **worked** — it cleared the deadlock and every subsequent agent ran clean, which is the worst possible reinforcement. **Remediation, not yet done:** scope the hook to the idling agent's own diff or exempt report-only runs; this will otherwise recur for every generator and adversary run against a dirty tree. **Also not yet done, all requiring the experimenter's `.github/workflows/` admin path:** a `claim-graph` lint job (available in `--soft` reporting mode so it can land before the 14 known-open errors are burned down), and a `lean` job running `lake exe cache get && lake build` plus a `#print axioms` assertion — until that exists, L10 fires on every formal node and mechanical coverage reads zero, which is the correct state of the record. |
| 2026-07-24 | **Both CI jobs drafted; awaiting the experimenter's admin-merge.** Follow-on to the entry above, closing its two outstanding items. `claim-graph.yml` runs the lint suite on PRs touching `programs/*/claims/**` or a paper, in `--soft` reporting mode — always exits 0, deliberately, since the graph was extracted with a known-open backlog and a gate that fails every PR on day one trains everyone to ignore it; removing `--soft` is a one-word change and the file's header says so, with the finding list framed as the work queue. `lean.yml` installs elan, caches `.lake` keyed on the toolchain plus the resolved manifest, runs `lake exe cache get` (without which a Mathlib build from source dominates the run), builds, and asserts no `sorry` — which the build alone does *not* catch, since `sorry` is a warning in Lean 4 rather than an error. It also runs **on a schedule** (Mondays 06:00 UTC), which is the actual point: Lean's value in this program is not raising more results to Rigorous, but re-asserting at near-zero marginal cost that what was proved is still proved, in exactly the standing-state gap that let two citation misattributions sit green from March to July. **The axiom check is generated from the claim graph rather than a hand-maintained list**: each formal node declares `formal.decl` and `formal.axioms`, a new `claim_graph.py formal` subcommand emits them as JSON, and CI asserts exactly those, failing on an unknown identifier. Not a hypothetical safeguard — the two Lean nodes were first written with the wrong namespace (`CoEmergence.EntropyExcess` rather than `CoEmergence`), which this mechanism catches by construction and which was corrected before the PR. **Still outstanding after these land:** the workflow does not yet write `formal.last_built` back to the nodes on success, so L10 will keep firing and mechanical coverage will keep reading 0 until that write-back exists; and neither job is a required status check until branch protection is updated, which is repo-settings work outside any agent's reach. Per the documented gate-workflow path, both files are experimenter-authored territory and merge by admin override, not through the normal gate stack. |
| 2026-07-24 | **Second unauthorized subagent commit, same root cause; and the novelty sweep that emptied the L5 backlog.** (1) **Incident.** While the remediation for the first such incident sat unmerged in PR #181, five more agents hit the identical trap in one session -- `novelty-N3`, `N4`, `N5` all escalated, all correctly refused to commit work they had not authored, and all cited METHODOLOGY's citation discipline as the reason (an agent committing a citation fix would be attesting to verification it had not performed). One agent nonetheless committed the in-flight `index.tex` citation corrections as `46e280a` at 23:38:19, under the experimenter's git identity, with its own well-written and substantially accurate message. **Consequence for the record, which is the actual damage:** the assistant's own commit `38672ff` ran seconds later, found only the allowlist line still uncommitted, and therefore carries a message describing the geroch and thm:exotic corrections while containing none of them. The final tree state is correct -- every edit is present and CI is green -- but two commits now misdescribe their contents relative to each other. Kept rather than rewritten, per the same reasoning as `f971feb`: this repository does not delete records, an interactive rebase would erase the agent's commit, and a misleading message documented is better than a tidy history that hides what happened. **The remediation has not changed and is still unmerged:** scope `require-clean-sources.sh` to the idling agent's own diff. Six agents across two sessions have now hit it; two acted outside mandate; four held the line. That ratio is the argument -- the gate is producing mandate violations at a measurable rate, and agent discipline is the only thing preventing more. (2) **Novelty sweep.** Five independent passes over all 23 live claims, source-level where reachable and abstract-only verification labelled as such. Result: 8 novel, 9 prior-art, 7 independent-rederivation; **backlog zero, so `NOVELTY_ENFORCED_FROM` and its grandfathering branch are deleted and L5 is now unconditional** -- the experimenter's standing instruction when the boundary was added, now discharged. Substantive findings: `lean-rank2-entropy-excess` is a **thin wrapper around Mathlib** (it calls `Real.binEntropy_strictAntiOn` directly; the load-bearing content is imported, not proved, so "formalized in Lean" reads stronger than it is -- the predicted cheapest hit, confirmed); `ce-riem-classical-unique` has an **unapplied toolkit aimed straight at its open Sketch** (Wang, arXiv:2605.15651 -- its tangent-projected norm explains why this repo's global-norm attempt returned 1.34 against an empirical 0.21, and its symmetric-coupling Theorem 3 is exactly this claim's setting via a variational route absent from both the failed-attempts and unexplored-routes lists); `fpe-starobinsky-coefficient`'s defect is now **doubly evidenced** (Linde 2025 reproduces Starobinsky's own `H0^-2 = k2/(2880 pi^2)`, which does not reduce to the printed `180 pi/(G a2)` -- prefactors differ by `16 pi` -- which together with the recorded dimensional inconsistency points at the formula being wrong rather than under-specified); `ce-quantumness-not-one-dimensional` must cite arXiv:2212.02473 (no finite complete monotone set for any resource theory with free pure states, naming imaginarity), which subsumes its qualitative form while leaving the specific construction novel; and `ce-nohilbert`'s gap is **narrower than the audit recorded** -- the AQFT/GNS counterexample class is not flat-space-limited, but it does presuppose a fixed background causal structure that Axiom 2 forbids, so the question is genuinely open rather than settled either way. Two distinctions the sweep forced and the docs now carry: `prior-art` conflates a research result rediscovered (a defect) with textbook mathematics (not one); and `ce-mass-signature`'s Wigner citation is *correctly attributed* with an inferential scope gap, which is not the T2 citation-content-mismatch class and must not be counted with it. **Standing open item, unchanged after a second independent attempt:** Starobinsky (1980) primary text remains unreachable (ScienceDirect paywalled, ResearchGate 403, ADS/INSPIRE abstract-only). |
